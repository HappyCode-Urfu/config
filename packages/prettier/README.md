# :milky_way: HappyCode prettier-config

## Установить конфиг
```sh
yarn add -D @happycode-core/prettier
```

## Как использовать
Создать файл `.prettierrc.js` и добавить в него содержимое:

```js
import { prettier } from '@happycode-core/prettier';

export default prettier();
```
